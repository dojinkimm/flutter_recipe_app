import 'package:flutter/material.dart';

class MyRef extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("내가 가지고 있는 재료")
      ),
    );
  }
}